# Before adding things like .DS_Store, ignores for your editors, etc, consider
# instead to use a global git ignore. This keeps the ignores here clean, and
# specific to this project - not your text editor, OS, or other tooling.
#
# Attempts to ignore files specific to your configuration will be rejected.
#
# Please justify all ignore entries with comments.

# Ignore various code injector (CSS/JS) generated emissions.
.tmp-css
.tmp-css-write
code-injector-rules.json
# Ignore NPM/Yarn generated dependencies.
node_modules/
# Ignore org-mode compiled emacs-lisp files.
org/*.el

# Cookies from curl usages.
wand-cookies.txt

# Hide all private files.
*.priv.txt
*.private.js

# Beause of the symlink, yas will compile these snippets and they wind up in
# this repository.
.yas-compiled-snippets.el

# Ignore fonts for now. We expand large zip files in there, and some of it is
# copyrighted and shouldn't go into a repository. Perhaps they could go into the
# private repository so they needn't be retrieved again?
fonts/

# Ignore scratch area. Currently used for font installation.
scratch/

# I need to fix these at some point, but for now just ignore them.
# Nix symlinks to builds.
result/
result
# Python cruft.
venv/

# Ignore stuff we might just remove anyways.
*.test-if-can-be-removed/

# Transient generated files, such as built or decompressed disk images, land
# under tmp/ to keep the repository root free of cruft.
/tmp/

# Ignore Nix generated SSH keys for the Darwin Linux-builder.
/keys

# Ignore the Linux-builder image itself.  It's not that big currently (19MB).
/nixos.qcow2
