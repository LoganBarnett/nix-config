#+title:     Secrets
#+author:    Logan Barnett
#+email:     logustus@gmail.com
#+date:      <2024-08-15 Thu>
#+language:  en
#+file_tags:
#+tags:

* Secrets in Nix

I've had a lot of luck with [[https://github.com/ryantm/agenix][agenix]] in conjunction with [[https://github.com/oddlama/agenix-rekey][agenix-rekey]].

In essence, secrets are encrypted using SSH keys, and those keys target a
particular host.  =agenix-rekey= specifically has some kind of chained or nested
encryption going on such that a secret can be encrypted for a specific host
automatically.  =agenix-rekey= can also generate secrets on the fly, which helps
keep everything very declarative.

Since SSH traffic is considered to be safe enough to go all the way across the
Internet, the community (and also myself, I suppose) has deemed it worthy of
placing these encrypted values into our public repositories.  I suspect part of
this is because there are challenges with getting a good Nix configuration going
with a mixed public and private setup.  If the SSH encryption methods are
cracked somehow, then these secrets will become public knowledge.

I did some reading a while back and saw claims that an SSH datagram could be
broken with a brute force attack that would take several billion years at our
current computing level.  Hopefully I'll have a better system by then.

** Bootstrapping secrets

One of the challenges that comes up with this setup is the bootstrapping
problem.  I've run into this a few times and I haven't come up with an
idiot-proof solution yet.  This document hopes to capture my learnings on the
topic.

For Raspberry Pi hosts this is solved: the host's real keypair exists before
the image is built, so the very first boot decrypts every secret.  See the
Raspberry Pi section of [[file:README.org][README.org]] for the flow.

*** Master key public half

The master identity is passphrase protected, so anything that needs to /encrypt/
to it would otherwise have to decrypt it first just to learn its recipient.
Committing the public half avoids that, and removes passphrase prompts from
encryption-only operations.  The file names are declared in
[[file:secrets/master-identity.nix][secrets/master-identity.nix]] - the single place aware of them; the commands
below show the current names.  Generate it once and commit it:

#+begin_src sh :results none :exports code
rage --decrypt secrets/agenix-master-key-3.age | rage-keygen -y \
  > secrets/agenix-master-key-3.pub
git add secrets/agenix-master-key-3.pub
#+end_src

If the installed ~rage-keygen~ refuses to read from stdin, pass ~/dev/stdin~
as its input file argument.

** no identity matched any of the recipients

Be sure to check for the initial error if secrets seem to be missing.  You may
find this:

#+begin_quote
age: error: no identity matched any of the recipients
#+end_quote

This means the destination identity is missing.  If this is a new host, this is
likely because the identity was never copied over in plain text.

This comes back to [[Bootstrapping secrets]].  Without a host key present, we have
no identity with which to supply to =age= to decrypt the remaining secrets.
We do provide the host key via =agenix=, but it's encrypted via its own value.

[[https://github.com/oddlama/][oddlama]], the author of =agenix-rekey=, has a [[https://github.com/oddlama/nix-config][nix-config]] repository whose README
shows some instructions for [[https://github.com/oddlama/nix-config?tab=readme-ov-file#add-new-machine][adding a new machine]] and the manual steps involved
(there are plenty).  I think if they are having to do these steps, I might be
out of luck on doing this myself.

** GPG signing subkey for headless development

Headless development hosts (e.g. rubidium) need a GPG signing subkey so commits
can be signed without the master secret key ever leaving the workstation.  The
subkey is an ed25519 signing-only key created under the master key
=41E46FB1ACEA3EF0=.

*** What gets deployed

- =gpg-keys/logan-gpg-public.asc= — full public keyring (master + all subkeys),
  committed to the repo in plaintext.
- =secrets/logan-gpg-signing-subkey.age= — the secret subkey only, encrypted
  with rage for agenix.  Decrypted at boot to =/run/agenix/logan-gpg-signing-subkey=.
- A systemd oneshot (=import-gpg-signing-key=) imports both into logan's GPG
  keyring on each boot.

*** Rehydrating the secret

If the =.age= file is lost, the subkey is revoked, or a new subkey is needed,
run the automated script from any host that has the master GPG key:

#+begin_src sh
gpg-dev-signing-key-setup
#+end_src

This prompts only for the master key passphrase and handles the rest: subkey
creation, public key export, secret subkey export, rage encryption, and
plaintext shredding.  It prints the new subkey fingerprint at the end.

After running the script:

1. Update =git-signing-key= in =users/logan-headless-development.nix= with the
   printed fingerprint.
2. Commit and push dotfiles.
3. In =nix-config-private=: ~nix flake update dotfiles~.
4. ~caffeinate proton-deploy switch rubidium~.

The script source lives at =scripts/gpg-dev-signing-key-setup= and is wrapped as
a Nix derivation in =derivations/gpg-dev-signing-key-setup.nix=.

** TODO Fix the master key

It's probably not a good idea to have a master identity whose password is the
only thing protecting itself.  Create a new identity, place it on inert storage
as a backup, and rekey everything.

** KILL Fix excessive prompting during generation/rekey operations
CLOSED: [2026-02-08 Sun 19:02]

Use this form to declare =masterIdentities= to prevent over prompting.

https://github.com/oddlama/agenix-rekey?tab=readme-ov-file#agerekeymasteridentities

Actually that is only for a mode of =agenix-rekey= that I'm not using.
